---
- name: Do installation thing
  ansible.builtin.shell:
    cmd: echo installation thing
  tags:
    - install

- name: Do ubuntu configure thing
  ansible.builtin.shell:
    cmd: echo configure thing
  tags:
    - configure
